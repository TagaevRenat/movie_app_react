const NotFound = () => {
    return (
        <div className="fs-1 text-center">You have landed on a non-existent page. To work with the application, follow any of the links in the navbar.</div>
    )
}

export default NotFound